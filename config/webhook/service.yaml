apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
  labels:
    app.kubernetes.io/name: driftwatch
    app.kubernetes.io/component: manager
spec:
  selector:
    app.kubernetes.io/name: driftwatch
    app.kubernetes.io/component: manager
  ports:
    - port: 443
      targetPort: webhook
      protocol: TCP
