apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # role.yaml is generated from the kubebuilder markers in
  # internal/controller/driftcheck_controller.go and committed, so a reviewer
  # can read the whole of what the manager can do without running
  # controller-gen. hack/verify-crd-docs.sh fails CI if it drifts from the
  # markers.
  - role.yaml
  - leader_election_role.yaml
  - role_binding.yaml
  - service_account.yaml
