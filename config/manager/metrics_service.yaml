apiVersion: v1
kind: Service
metadata:
  name: driftwatch-metrics
  namespace: system
  labels:
    app.kubernetes.io/name: driftwatch
    app.kubernetes.io/component: manager
spec:
  selector:
    app.kubernetes.io/name: driftwatch
    app.kubernetes.io/component: manager
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
